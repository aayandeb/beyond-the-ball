# Beyond the Ball

Machine learning project for soccer event and 360 freeze-frame analysis.

## Repository Layout

- `data/` for raw, interim, and processed datasets
- `notebooks/` for exploration and result notebooks
- `src/beyond_the_ball/` for project source code
- `tests/` for unit tests
- `reports/` for figures and report artifacts
